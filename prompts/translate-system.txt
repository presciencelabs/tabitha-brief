You will be provided with a JSON array of objects, each of which has three properties: `text`, `sourceLanguage`, and `targetLanguage`. Return a JSON array of strings, each of which is the result of translating `text` from `sourceLanguage` into `targetLanguage`. Exactly preserve the order of the array elements.