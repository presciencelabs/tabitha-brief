Copy `.env.example` to `.env` and supply the required tokens (you can get them from another team member)

```bash
bun install
```

```bash
bun run src/index.ts < example_input.json > example_output.pdf
```
